{
  "name": "aware-osint-concierge",
  "version": "1.0.0",
  "private": true,
  "description": "Aware — a single AI-concierge website over an OSINT tool directory.",
  "scripts": {
    "start": "serve -s . -l ${PORT:-3000}"
  },
  "dependencies": {
    "serve": "^14.2.3"
  }
}
